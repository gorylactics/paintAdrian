<%= form_with(model: zombie1, local: true) do |form| %>
  <% if zombie1.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(zombie1.errors.count, "error") %> prohibited this zombie1 from being saved:</h2>

      <ul>
      <% zombie1.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :humanoMordido1 %>
    <%= form.text_field :humanoMordido1 %>
  </div>

  <div class="field">
    <%= form.label :integer %>
    <%= form.text_field :integer %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
